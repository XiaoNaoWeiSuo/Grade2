/// 本地敏感配置模板。
/// 复制此文件为 app_secrets.dart 并填写真实值后即可编译运行。
class AppSecrets {
  /// SMTP 邮箱账号（用于用户反馈与登录日志上报）
  static const String smtpUser = '';

  /// SMTP 授权码
  static const String smtpPassword = '';
}
